#include "BoolTriplesGen.h"
#include "VoleGen.h"
#include "RotGen.h"
#include "BeaverTriplesGen.h"
#include "../shuffle/ShareCorrelationGen.h"
#include "../shuffle/MShuffle.h"


void MPSIpreGen(u32 idx, u32 numParties, u32 logNum, u32 numThreads = 1);

void MPSICpreGen(u32 idx, u32 numParties, u32 logNum, u32 numThreads = 1);

void MPSICSpreGen(u32 idx, u32 numParties, u32 logNum, u32 numThreads = 1);

void MPSUpreGen(u32 idx, u32 numParties, u32 logNum, u32 numThreads = 1);


